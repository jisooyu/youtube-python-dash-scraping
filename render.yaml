services:
  - type: web
    name: finance-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python dashboard_statistics.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.1
